{
  "name": "cloud-messaging-dashboard",
  "version": "1.0.0",
  "description": "Real-time messaging application with React and Node.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "keywords": ["react", "nodejs", "websocket", "real-time", "messaging"],
  "author": "Your Name",
  "license": "MIT"
}